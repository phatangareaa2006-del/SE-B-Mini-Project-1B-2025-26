# Set this to your laptop's local IP address for phone access
# Find your IP: run `ipconfig` (Windows) or `ifconfig` (Mac/Linux)
# Both devices must be on the same WiFi
VITE_API_URL=http://192.168.0.117:5000
